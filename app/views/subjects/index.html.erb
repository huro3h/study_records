<p style="color: green"><%= notice %></p>

<h1>科目一覧</h1>

<div id="subjects">
  <% @subjects.each do |subject| %>
    <%= render subject %>
    <p>
    </p>
  <% end %>
</div>

<%= link_to "科目を追加する", new_subject_path %>
